'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});

var _typeof = typeof Symbol === "function" && typeof Symbol.iterator === "symbol" ? function (obj) { return typeof obj; } : function (obj) { return obj && typeof Symbol === "function" && obj.constructor === Symbol && obj !== Symbol.prototype ? "symbol" : typeof obj; };

exports.default = serializeOptions;
/**
 *
 * Function that transforms string options into object options with keys of
 * `key` and `value`
 *
 * @name serializeOptions
 * @type Function
 * @param {Array} optionArray - Array of option values
 * @returns {Array} Array of object options
 *
 * @example
 *
 * const options = ['apple', 'banana']
 *
 * serializeOptions(options)
 *
 * // [{ key: 'apple', value: 'apple' }, { key: 'banana', value: 'banana' }]
 *
**/

function serializeOptions(optionArray) {
  return optionArray.map(function (option) {
    return (typeof option === 'undefined' ? 'undefined' : _typeof(option)) === 'object' ? option : { key: option, value: option };
  });
}